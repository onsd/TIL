version: '3'
services:
  ruby:
    image: ruby
    container_name: ruby 
    restart: always
    ports:
      - 8080:8080
    # environment:
    volumes:
      - ./:/workspace
    command: /bin/sh -c "while :; do sleep 10; done"
